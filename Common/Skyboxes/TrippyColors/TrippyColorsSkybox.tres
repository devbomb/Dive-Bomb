[gd_resource type="Environment" format=3 uid="uid://dk0eyoi3afr5p"]

[sub_resource type="Shader" id="Shader_1j2jw"]
code = "shader_type sky;

uniform sampler3D red_noise : filter_linear, source_color, hint_default_black;
uniform sampler3D green_noise : filter_linear, source_color, hint_default_black;
uniform sampler3D blue_noise : filter_linear, source_color, hint_default_black;

float get_brightness(sampler3D noise, vec2 sky_coords)
{
	const float time_scale = 0.01;
	vec4 color = texture(noise, vec3(sky_coords.x, sky_coords.y, TIME * time_scale));
	return length(color.rgb);
}

vec3 hsv2rgb(float h_degrees, float s, float v)
{
	h_degrees = mod(h_degrees, 360.0);
	float c = v * s;
	float x = c * (1.0 - abs(mod(h_degrees / 60.0, 2.0)) - 1.0);
	float m = v - c;
	
	vec3 rgb;
	if (h_degrees < 60.0) rgb = vec3(c, x, 0.0);
	else if (h_degrees < 120.0) rgb = vec3(x, c, 0.0);
	else if (h_degrees < 180.0) rgb = vec3(0.0, c, x);
	else if (h_degrees < 240.0) rgb = vec3(0.0, x, c);
	else if (h_degrees < 300.0) rgb = vec3(c, 0.0, x);
	else rgb = vec3(c, x, 0);
	
	return rgb + m;
}

void sky() 
{
	float hue_degrees = get_brightness(red_noise, SKY_COORDS) * 360.0;
	COLOR = hsv2rgb(hue_degrees, 0.5, 1.0);
}
"

[sub_resource type="FastNoiseLite" id="FastNoiseLite_mkb3s"]
seed = 2
frequency = 0.0551

[sub_resource type="NoiseTexture3D" id="NoiseTexture3D_n0vaf"]
noise = SubResource("FastNoiseLite_mkb3s")
seamless = true
seamless_blend_skirt = 0.493

[sub_resource type="FastNoiseLite" id="FastNoiseLite_1j2jw"]
seed = 1
frequency = 0.0551

[sub_resource type="NoiseTexture3D" id="NoiseTexture3D_vv06q"]
noise = SubResource("FastNoiseLite_1j2jw")
seamless = true
seamless_blend_skirt = 0.493

[sub_resource type="FastNoiseLite" id="FastNoiseLite_n0vaf"]
frequency = 0.0551

[sub_resource type="NoiseTexture3D" id="NoiseTexture3D_mkb3s"]
noise = SubResource("FastNoiseLite_n0vaf")
seamless = true
seamless_blend_skirt = 0.493

[sub_resource type="ShaderMaterial" id="ShaderMaterial_n0vaf"]
shader = SubResource("Shader_1j2jw")
shader_parameter/red_noise = SubResource("NoiseTexture3D_mkb3s")
shader_parameter/green_noise = SubResource("NoiseTexture3D_vv06q")
shader_parameter/blue_noise = SubResource("NoiseTexture3D_n0vaf")

[sub_resource type="Sky" id="Sky_mkb3s"]
sky_material = SubResource("ShaderMaterial_n0vaf")

[resource]
background_mode = 2
sky = SubResource("Sky_mkb3s")
